# Optional local SQLite path override.
# Leave empty to use the default: ${HOME}/.local/viki/confluence/replica.db
DATABASE_PATH=

# Optional overrides for Ollama embeddings
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_EMBED_MODEL=nomic-embed-text

# Optional overrides for manual bootstrap, sync, or rebuild
PARENT_IDS=925576634,776923811

# Optional Keychain defaults for `make update-token`
KEYCHAIN_SERVICE=codex_confluence_pat
KEYCHAIN_ACCOUNT=
