database:
  dsn: "postgres://postgres:postgres@127.0.0.1:1/confluence_replica?sslmode=disable"

confluence:
  base_url: "https://example.invalid"
  token: "keychain://codex_confluence_pat"

logging:
  level: "ERROR"

embeddings:
  provider: "noop"
  model: ""
