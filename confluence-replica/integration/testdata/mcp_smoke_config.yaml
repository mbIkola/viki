database:
  path: "/tmp/confluence-replica-mcp-smoke.db"

confluence:
  base_url: "https://example.invalid"
  token: "keychain://codex_confluence_pat"

logging:
  level: "ERROR"

embeddings:
  provider: "noop"
  model: ""
